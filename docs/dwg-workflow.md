# DWG / 图纸工作流

V1 识别 DWG/DXF 并将其保留为原始图纸资源，不解析 CAD 实体，也不依赖 AutoCAD。

推荐流程：在 AutoCAD、DWG TrueView 或客户指定工具中将图纸导出为 PDF 或 PNG；在资料预检中确认该导出文件类型正确；在模板中为“电气一次系统图”等字段建立批注，并在字段映射中记录导出图纸的来源。自动嵌入图片和 PDF 页面是下一轮基于真实模板的增强重点。
